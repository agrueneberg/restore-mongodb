restore-mongodb
===============

A MongoDB adapter for reStore, a remoteStorage server.

How to run the tests
--------------------

- Start MongoDB
- `git submodule update --init`
- `npm install`
- `npm test`
